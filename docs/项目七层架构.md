# DrinkMind 项目七层架构

这份文档记录的是当前代码的真实状态，不是理想架构图，也不是简历包装。

当前后端已经完成 API、service、db、rules、knowledge、workflows 的第一轮分层整理。现在大体是：

```text
backend/main.py              app 启动入口
backend/api/schemas.py       API 请求模型
backend/api/routers/         Controller 层
backend/services/            业务服务层
backend/agents/              AI / Agent / Reasoning 能力
backend/rules/               确定性规则和本地估算
backend/knowledge/           知识库、RAG、Chroma、采集
backend/workflows/           LangGraph / workflow 编排
backend/db/database.py       ORM models、DB session、migration 真实实现
backend/scripts/             后端维护脚本
```

简单说：现在请求先进 router，router 调 service，service 再调 agents、rules、knowledge、workflows 和 db。`main.py` 已经不再堆业务流程。

## 1. Interface / API 层

### 这一层负责什么

这一层负责接住前端请求，并把请求交给后面的业务服务。

它应该做：

- 定义接口路径。
- 接收 request。
- 注入数据库 session。
- 调用 service。
- 返回 service 给的结果。

它不应该做很多业务判断，也不应该直接写一大段数据库流程。

### 当前项目对应哪些文件

- `backend/main.py`
- `backend/api/schemas.py`
- `backend/api/routers/logs.py`
- `backend/api/routers/knowledge.py`
- `backend/api/routers/agent.py`
- `backend/api/routers/health.py`

### 这些文件现在是否已经在合适位置

基本合适。

`main.py` 现在主要负责：

- 创建 `FastAPI` app。
- 配置 CORS。
- 初始化数据库。
- 注册 routers。
- 挂载静态文件。
- 本地启动时跑 `uvicorn`。

`api/schemas.py` 现在放 request schema，也就是接口入参模型。

`api/routers/*` 现在主要是 Controller。它们还不是完全零逻辑，但已经比之前清楚很多。

### 如果以后重构，建议迁移到哪里

这层目前不用大迁移。

后面如果继续整理，可以做：

- 检查 router 里是否还有少量可以下沉的 helper。
- router 里重复的 endpoint helper 再慢慢下沉到 service。
- schema 如果变太多，可以按业务拆成多个 schema 文件。

### 注意事项

- API path 不要随便改，前端已经依赖这些路径。
- response shape 不要随便改，测试和前端都可能依赖。
- router 里不要再新增大段业务流程，优先写到 service。

## 2. Orchestration / Workflow 层

### 这一层负责什么

这一层负责“流程怎么走”。

它决定：

- 先解析用户输入，还是先查知识库。
- 命中知识库后怎么处理。
- 估算失败时怎么降级。
- 用户意图是记录饮品、聊天建议，还是生成健康计划。
- 哪些 Agent 或工具要参与。

### 当前项目对应哪些文件

- `backend/agents/orchestrator.py`
- `backend/workflows/nutrition_pipeline.py`
- `backend/services/agent_action_service.py`
- `backend/services/chat_service.py`
- `backend/services/drink_log_service.py`
- `backend/services/knowledge_acquisition_service.py`

### 这些文件现在是否已经在合适位置

大体可以接受。

`orchestrator.py` 和 `nutrition_pipeline.py` 仍在 `agents` 目录里，因为它们和 Agent 能力关系很近。

service 层现在承担了不少业务流程编排，比如：

- 创建饮品记录时调用营养估算。
- 聊天时保存消息、解析 intake、生成回复、写 trace。
- agent act 时调用 orchestrator、memory、health plan。
- evidence 审核后写入正式知识库。

### 如果以后重构，建议迁移到哪里

当前不用新建很重的 `workflows` 目录。

如果以后流程越来越多，可以考虑：

```text
backend/workflows/nutrition_workflow.py
backend/workflows/agent_workflow.py
```

但现在先放在 `agents` 和 `services` 里就够了。

### 注意事项

- 不要为了分层强行把一个清楚的流程拆碎。
- `nutrition_pipeline.py` 的行为很关键，composition eval 会保护它。
- `orchestrator.py` 暂时不要动内部行为。

## 3. Agent / Reasoning 层

### 这一层负责什么

这一层负责理解、推理、生成。

包括：

- 解析自然语言饮品。
- 判断用户意图。
- 生成陪伴式聊天回复。
- 生成健康计划。
- 生成日报和周报。
- 从用户话语里提取记忆。

### 当前项目对应哪些文件

- `backend/agents/intake_parser.py`
- `backend/agents/companion_agent.py`
- `backend/services/nutrition_estimation_service.py`
- `backend/agents/orchestrator.py`
- `backend/agents/health_plan_agent.py`
- `backend/agents/report_agent.py`
- `backend/services/memory_service.py`
- `backend/agents/llm_config.py`

### 这些文件现在是否已经在合适位置

暂时可以接受。

不完美的地方是：

- `memory_service.py` 既做记忆逻辑，也直接碰数据库。
- `health_plan_agent.py` 既生成计划，也保存计划。
- `report_agent.py` 更像报告服务，但现在还在 agents。
- `llm_config.py` 更像基础配置。

这些现在不是最大问题，可以暂缓。

### 如果以后重构，建议迁移到哪里

可以先不动。

以后如果要更清楚，可以考虑：

```text
backend/services/memory_service.py
backend/core/llm.py
```

`health_plan_service.py` 和 `report_service.py` 已经存在；后面如果继续拆，重点不是再建 service 文件，而是判断 `health_plan_agent.py` 和 `report_agent.py` 里面哪些是纯生成能力，哪些是数据库流程。

### 注意事项

- 不要把可离线跑的解析逻辑改成必须调用大模型。
- 不要改 LangGraph 或 orchestrator 行为。
- Agent 层现在是核心能力层，先保护行为稳定。

## 4. Tools / Rules / Retrieval 层

### 这一层负责什么

这一层负责可复用的工具能力。

包括：

- 成分估算。
- 糖和咖啡因规则。
- 风险提示。
- 知识库查找。
- RAG / Chroma 同步和检索。
- 图片分析和证据抽取。

### 当前项目对应哪些文件

- `backend/rules/local_estimator.py`
- `backend/rules/composition_agent.py`
- `backend/rules/ingredient_rules.py`
- `backend/rules/risk_agent.py`
- `backend/knowledge/knowledge_lookup.py`
- `backend/knowledge/rag_store.py`
- `backend/knowledge/knowledge_acquisition_agent.py`

### 这些文件现在是否已经在合适位置

现在位置已经比较清楚。

`composition_agent.py` 和 `ingredient_rules.py` 已经放到 `rules`。

`knowledge_lookup.py`、`rag_store.py` 和 `knowledge_acquisition_agent.py` 已经放到 `knowledge`。

`knowledge_acquisition_agent.py` 现在仍然比较大，里面有搜索、图片导入、候选、证据、审核相关能力。

### 如果以后重构，建议迁移到哪里

以后如果继续拆，可以考虑把文件名再改得更短：

```text
backend/rules/composition.py
backend/rules/ingredients.py
backend/rules/risk.py
backend/knowledge/lookup.py
backend/knowledge/rag_store.py
backend/knowledge/acquisition.py
```

现在先不急。

### 注意事项

- composition 和 nutrition pipeline 是项目核心，不要顺手改公式。
- RAG / Chroma 行为不要在普通分层整理里改。
- knowledge acquisition 逻辑复杂，拆之前要先补好验收点。

## 5. Knowledge / Memory 层

### 这一层负责什么

这一层负责项目“知道什么”和用户“长期偏好是什么”。

包括：

- 正式饮品知识库。
- 候选饮品。
- 营养证据。
- 用户偏好。
- 聊天历史。
- 饮品日志。
- 睡眠记录。
- Agent trace。
- Chroma 向量库。

### 当前项目对应哪些文件

- `backend/db/database.py`
- `backend/services/knowledge_service.py`
- `backend/services/knowledge_acquisition_service.py`
- `backend/services/user_preference_service.py`
- `backend/knowledge/knowledge_lookup.py`
- `backend/knowledge/rag_store.py`
- `backend/knowledge/knowledge_acquisition_agent.py`
- `backend/services/memory_service.py`
- `backend/scripts/init_rag.py`
- `backend/chroma_db`
- `drinks.db`

### 这些文件现在是否已经在合适位置

部分合适。

知识库 CRUD 和 acquisition 工作流已经进了 service。

用户偏好入口也已经从 router 抽到了 `user_preference_service.py`。

`backend/db/database.py` 现在是数据库真实实现，里面放了：

- ORM models。
- DB session。
- init_db。
- 自动迁移。

旧的 `backend/database.py` 已经删除。现在项目内部直接从 `db.database` import。

### 如果以后重构，建议迁移到哪里

以后如果要继续拆，可以从 `backend/db/database.py` 再拆成：

```text
backend/db/session.py
backend/db/models/
backend/db/migrations.py
```

当前阶段先不继续拆 `backend/db/database.py` 内部结构。

### 注意事项

- 现在 services 直接用 SQLAlchemy，这是有意保留的。
- 当前阶段不抽 repository。
- 继续拆 `backend/db/database.py` 风险比拆 router/service 高，需要单独做。
- SQL 知识库和 Chroma 要保持同步。

## 6. Guardrails / Verification 层

### 这一层负责什么

这一层负责让输入输出不要太离谱。

包括：

- request 字段范围。
- 图片大小和类型限制。
- 来源域名限制。
- feedback 修正值校验。
- 估算结果解释。
- 离线模式控制。
- 风险和提醒。

### 当前项目对应哪些文件

- `backend/api/schemas.py`
- `backend/services/nutrition_feedback_service.py`
- `backend/services/knowledge_acquisition_service.py`
- `backend/workflows/nutrition_pipeline.py`
- `backend/knowledge/knowledge_acquisition_agent.py`
- `backend/agents/llm_config.py`
- `backend/rules/risk_agent.py`

### 这些文件现在是否已经在合适位置

基本可接受。

现在校验分布在 schema、service、agent 里：

- 请求形状在 `api/schemas.py`。
- feedback 业务校验在 `nutrition_feedback_service.py`。
- acquisition 请求限制在 `knowledge_acquisition_service.py` 和 agent 里。
- LLM 开关在 `llm_config.py`。

### 如果以后重构，建议迁移到哪里

暂时不建议单独建一个很重的 guardrails 目录。

以后如果校验越来越多，可以考虑：

```text
backend/services/validation_service.py
backend/rules/risk.py
```

### 注意事项

- 错误码和错误信息不要随便变。
- feedback 的 corrected values 校验不能丢。
- 离线控制不能丢，测试会覆盖。
- 解释字段会影响前端展示。

## 7. Evaluation / Observability 层

### 这一层负责什么

这一层负责看系统有没有跑对，以及出问题时能不能追踪。

包括：

- 单元测试。
- API 测试。
- composition eval。
- Agent trace。
- nutrition graph trace。
- quality gate。

### 当前项目对应哪些文件

- `backend/tests/`
- `backend/tests/run_composition_eval_report.py`
- `backend/tests/fixtures/composition_eval_cases.json`
- `backend/services/trace_service.py`
- `backend/db/database.py` 里的 `AgentTrace`
- `backend/workflows/nutrition_pipeline.py` 里的 `graph_trace`
- `scripts/quality_gate.ps1`

### 这些文件现在是否已经在合适位置

比之前清楚很多。

trace 保存和查询已经从 router 抽到 `trace_service.py`。

测试仍然集中在 `backend/tests`，这没问题。

### 如果以后重构，建议迁移到哪里

当前不急。

以后如果 eval 变多，可以考虑：

```text
backend/evals/
```

但现在 `backend/tests/run_composition_eval_report.py` 继续保留即可。

### 注意事项

- 每次改 nutrition pipeline 都要跑 composition eval。
- 每次改 API 层都要跑 quality gate。
- `AgentTrace` 是看 agent 行为的重要入口，不要删。

## 当前实际目录分层

### `backend/main.py`

现在是 app bootstrap。

它负责启动和注册，不负责具体业务。

### `backend/api/schemas.py`

这是 API DTO / request schema。

现在所有主要 Pydantic request model 都从 `main.py` 移到了这里。

### `backend/api/routers/*`

这是 Controller 层。

当前有：

- `logs.py`
- `knowledge.py`
- `agent.py`
- `health.py`

### `backend/services/*`

这是业务服务层。

当前有：

- `drink_log_service.py`
- `nutrition_feedback_service.py`
- `knowledge_service.py`
- `knowledge_acquisition_service.py`
- `trace_service.py`
- `chat_service.py`
- `agent_action_service.py`
- `user_preference_service.py`
- `health_service.py`
- `health_plan_service.py`
- `report_service.py`

### `backend/agents/*`

这是 AI / Agent / Reasoning 能力层。

当前主要放：

- LLM 调用。
- intake parsing。
- memory。
- health plan。
- report。

规则、知识库和 workflow 已经分别移到 `rules`、`knowledge`、`workflows`。

### `backend/rules/*`

这是确定性规则和本地估算层。

当前有：

- `composition_agent.py`
- `ingredient_rules.py`
- `local_estimator.py`
- `risk_agent.py`

### `backend/knowledge/*`

这是知识库、RAG、Chroma 和采集层。

当前有：

- `knowledge_lookup.py`
- `rag_store.py`
- `knowledge_acquisition_agent.py`

### `backend/workflows/*`

这是 workflow 层。

当前有：

- `nutrition_pipeline.py`

### `backend/db/database.py`

这是当前 DB 真实实现。

它仍然负责：

- ORM models。
- DB session。
- init_db。
- migration / schema repair。

后面可以继续拆 models、session、migration，但现在先不动。

### `backend/scripts/init_rag.py`

这是后端维护脚本。

它负责把 SQL 里的 `DrinkKnowledge` 同步到 Chroma，或者从 `knowledge_base.json` 初始化知识库。

## 当前 Router 到 Service 映射

### `logs.py`

调用：

- `drink_log_service`
- `nutrition_feedback_service`

主要负责：

- 饮品日志。
- 睡眠日志。
- 删除记录。
- 同步记录。
- nutrition feedback。

### `knowledge.py`

调用：

- `knowledge_service`
- `knowledge_acquisition_service`

主要负责：

- 正式知识库 CRUD。
- candidate CRUD。
- evidence CRUD。
- evidence approve。
- bulk approve / delete。
- acquisition discover。
- image import。

### `agent.py`

调用：

- `agent_action_service`
- `chat_service`
- `trace_service`
- `user_preference_service`

主要负责：

- `/api/agent/parse_intake`
- `/api/agent/act`
- `/api/chat`
- `/api/agent/traces`
- `/api/user/preferences`

### `health.py`

调用：

- `health_service`
- `health_plan_service`
- `report_service`

主要负责：

- daily insights。
- health plan。
- daily report。
- weekly report。

`health.py` 已经完成 service 化，现在也是 thin controller。

## 当前核心业务链路

### 手动记录饮品链路

大概流程：

```text
前端提交饮品
-> logs.py
-> drink_log_service.create_drink_log
-> workflows.nutrition_pipeline.estimate_drink_nutrition
-> db.database.DrinkLog
-> 返回记录和营养估算结果
```

要读的文件：

- `backend/api/routers/logs.py`
- `backend/services/drink_log_service.py`
- `backend/workflows/nutrition_pipeline.py`
- `backend/db/database.py`

### Nutrition LangGraph estimation 链路

大概流程：

```text
drink_log_service
-> estimate_drink_nutrition
-> knowledge lookup
-> composition estimation
-> risk / explanation / graph trace
-> 返回 caffeine、sugar、warnings、explainability
```

要读的文件：

- `backend/workflows/nutrition_pipeline.py`
- `backend/knowledge/knowledge_lookup.py`
- `backend/rules/composition_agent.py`
- `backend/rules/ingredient_rules.py`
- `backend/rules/risk_agent.py`

### feedback -> evidence -> knowledge 链路

大概流程：

```text
用户修正营养值
-> logs.py
-> nutrition_feedback_service
-> 更新 DrinkLog explainability
-> 可选创建 ProductCandidate
-> 创建 NutritionEvidence
-> knowledge.py approve evidence
-> knowledge_acquisition_service
-> 写入 DrinkKnowledge
-> sync Chroma
```

要读的文件：

- `backend/api/routers/logs.py`
- `backend/services/nutrition_feedback_service.py`
- `backend/api/routers/knowledge.py`
- `backend/services/knowledge_acquisition_service.py`
- `backend/services/knowledge_service.py`
- `backend/knowledge/rag_store.py`

### chat / agent act 链路

聊天大概流程：

```text
POST /api/chat
-> agent.py
-> chat_service
-> 保存用户消息
-> intake_parser 判断是否记录饮品
-> companion_agent 或 intake parse response
-> memory_service
-> 保存 assistant 消息
-> trace_service
```

agent act 大概流程：

```text
POST /api/agent/act
-> agent.py
-> agent_action_service
-> orchestrator
-> memory_service
-> health_plan_agent
-> trace_service
```

要读的文件：

- `backend/api/routers/agent.py`
- `backend/services/chat_service.py`
- `backend/services/agent_action_service.py`
- `backend/agents/intake_parser.py`
- `backend/agents/orchestrator.py`
- `backend/agents/companion_agent.py`
- `backend/services/memory_service.py`
- `backend/services/trace_service.py`

### knowledge acquisition 链路

大概流程：

```text
前端发起 discover / image import / manual candidate
-> knowledge.py
-> knowledge_acquisition_service
-> knowledge_acquisition_agent
-> ProductCandidate / NutritionEvidence
-> approve 后写入 DrinkKnowledge
-> rag_store 同步 Chroma
```

要读的文件：

- `backend/api/routers/knowledge.py`
- `backend/services/knowledge_acquisition_service.py`
- `backend/knowledge/knowledge_acquisition_agent.py`
- `backend/services/knowledge_service.py`
- `backend/knowledge/rag_store.py`

## 暂时不拆的东西

### `backend/db/database.py` 暂时不继续拆

它确实仍然偏大，但 Phase 3A 只做了移动，没有继续拆 models、session、migration。

现在先不继续拆，是为了避免一次性影响太多 model import、建表和测试。

### services 暂时直接用 SQLAlchemy

当前没有抽 repository。

这是刻意保持简单。现在项目规模还可以接受，先把 router 变薄更重要。

### agents 目录暂时保留生成和推理能力

`agents` 里现在主要还有：

- LLM。
- memory。
- intake parsing。
- companion chat。
- health plan。
- report。

这些文件里还有一些 DB 流程，但先不继续拆。下一步如果要拆，应该优先看 `memory_service.py`、`health_plan_agent.py`、`report_agent.py` 里哪些逻辑应该下沉到 service。

旧的 `backend/agent.py` 已经删除。新代码应该直接 import 真实模块：

- `backend/api/routers`
- `backend/services`
- `backend/agents`
- `backend/rules`
- `backend/knowledge`
- `backend/workflows`

## 下一步可选优化

### 1. `backend/db/database.py` 拆 models / session / migration

这一步有风险，建议单独做。

目标是拆内部结构，不是再移动入口文件。

### 2. 清理 Pydantic `.dict()` warning

现在 quality gate 会出现 Pydantic v2 的 `.dict()` deprecation warning。

这不是行为 bug，但后面可以统一改成 `model_dump()`。

### 3. agents 内部继续瘦身

`rules`、`knowledge`、`workflows` 已经拆出来了。

后续可以继续看 `memory_service.py`、`health_plan_agent.py`、`report_agent.py`，判断里面哪些数据库流程应该移到 service。

## 我应该从哪里开始读代码

如果你想理解项目，不建议从 `backend/db/database.py` 一头扎进去。

推荐顺序：

1. 先读 `backend/main.py`，知道 app 怎么启动、router 怎么挂上去。
2. 再读 `backend/api/routers/logs.py`，看手动记录饮品的入口。
3. 接着读 `backend/services/drink_log_service.py`，看记录怎么创建、怎么调用营养估算。
4. 再读 `backend/workflows/nutrition_pipeline.py`，看咖啡因和糖怎么估。
5. 然后读 `backend/api/routers/agent.py`，看聊天和 agent action 的入口。
6. 接着读 `backend/services/chat_service.py` 和 `backend/services/agent_action_service.py`。
7. 再读 `backend/agents/orchestrator.py`、`intake_parser.py`、`companion_agent.py`。
8. 想看知识库，再读 `backend/api/routers/knowledge.py`。
9. 接着读 `backend/services/knowledge_service.py` 和 `knowledge_acquisition_service.py`。
10. 最后再读 `backend/db/database.py`，把表结构和前面的流程对上。

如果只想先搞懂主流程，就先看：

```text
main.py
-> logs.py
-> drink_log_service.py
-> nutrition_pipeline.py
-> db/database.py
```
